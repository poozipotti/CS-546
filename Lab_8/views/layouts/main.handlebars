<!DOCTYPE html>

<head>
    <meta charset="utf-8">
    <title>My Site</title>
    <link rel="stylesheet" href="/public/css/main.css">
    <script src="/public/javascript/test.js" ></script>
</head>

<body>
    <header>
        <h1>Pallindrome Checker</h1>
    </header>
    <main>
        <label >
        Pallindrome:
        <form method=post action="/pallindromes" id=pallindromeForm>
            <input name="pallindrome"" type="text" placeholder="please enter a pallindrome" class="enterHere" value={{pallindrome.pallindrome}}>
        </label>
        <button type = "submit">submit</button>
        </form>
        {{#if hasErrors}}
            <ul class="error-list">
                {{#each errors}}
                    <li>
                    {{this}}
                    </li>
                {{/each}}
            </ul>
        {{/if}}
        {{{ body}}}
    </main>
    <footer>
        <p>By George Beckert, CWID 10399497 &copy; 2017 </p>
    </footer>
</body>

</html>

