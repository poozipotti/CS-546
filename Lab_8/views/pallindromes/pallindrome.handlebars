<h2>past pallindromes</h2>
<h3 id="wrongPallindromeLegend" class="legend">red = not a pallindrome</h3><h3 id="rightPallindromeLegend" class = "legend">blue = a pallindrome</h3>
<ul>
{{#each body}}
    {{#if isPallindrome }}
        <li class="pallindrome">{{pallindrome}}</li>
    {{else}}
        <li class="nonpallindrome">{{pallindrome}}</li>
    {{/if}}
{{/each}}
</ul>
